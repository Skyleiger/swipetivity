import 'package:flutter/material.dart';

class SurveysPage extends StatelessWidget {
  const SurveysPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Surveys Page is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
